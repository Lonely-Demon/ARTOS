# 01 — Source Archive

This area preserves the original project evidence used to understand the collaboration model and prior work. It is a provenance archive, not a place for current interpretations.

- `conversations/` contains the nine original project conversation exports.
- `snapshots/` contains the nine sequential reasoning snapshots.
- `consolidation/` contains the original comprehensive report and operational extract.

Do not rewrite these files to reflect later architecture decisions. Add derived interpretations under the appropriate canonical or methodology directory and link back to the source path.
