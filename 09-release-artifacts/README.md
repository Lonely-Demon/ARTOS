# 09 — Release Artifacts

This directory contains packaged exports of repository states, such as the operating-kernel reference ZIP. These bundles are convenient for transfer or review but are never the only copy of source material.

A release bundle should identify the source commit, generation date, included scope, and known omissions. Reproducible runtime databases, caches, and bytecode remain excluded unless a specific experiment requires them and the evidence boundary is documented.
